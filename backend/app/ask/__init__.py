"""Natural-language questions about the cafe, answered by fixed reports.

The model decides *which* report, dish and period a question refers to; the
code resolves those into exact bounds and computes every number. Nothing the
model emits is ever shown as a figure.
"""
